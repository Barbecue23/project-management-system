
 <h1><%= "รายชื่อนักศึกษาที่ดูแล" %></h1> 

<!-- Tabs -->
<div class="tabs">
 
</div>

<!-- Search + Filters -->
<div class="actions">
  <%# <input type="text" placeholder="Search" class="search-bar" />
  <button class="filter-btn">🔍 Filters</button> %>
</div>

<!-- Table -->
<div class="table-container">
  <table class="table">
    <thead>
      <tr>
        <th>ชื่อ-สกุล</th>
        <th>รหัสนักศึกษา</th>
        <th>เทอม</th>
        <th>วันที่อัพเดต</th>
        <th>สถานะ</th>
        <th>action</th>
      </tr>
    </thead>
    <tbody>
      <% @student.each do |student| %>
        <tr>
          <td><%= student.user.name %></td>
          <td><%= student.user.student_id %></td> 
          <td><%= student.year_term %></td>
          <td><%= student.updated_at.in_time_zone("Bangkok").strftime("%d/%m/%Y  %H:%M") %></td>
          <td>
            <% if student.status == "pending" %>
              <span class="badge badge-warning">รอการอนุมัติ</span>
            <% elsif student.status == "approved" %>
              <span class="badge badge-success">อนุมัติ</span>
            <% else %>
              <span class="badge badge-danger">ปฏิเสธ</span>
            <% end %>
          </td>
          <% if student.status != "rejected" %>
          <td>
            <%= button_to "ปฏิเสธ", delete_student_request_path(id: student.id),method: :delete, class: "btn btn-danger" %>
          </td>
          <% end %>
        </tr>
      <% end %> 
    </tbody>
  </table>
</div>
