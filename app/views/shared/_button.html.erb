<%= button_tag class: "px-8 py-2 rounded-md text-center flex #{style_classes} #{disabled ? 'opacity-50 cursor-not-allowed' : 'hover:bg-opacity-80'}", disabled: disabled do %>
  <% if icon %>
    <%= icon %>
  <% end %>
  <%= text %>
<% end %>

